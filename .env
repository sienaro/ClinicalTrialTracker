# NON-SECRET config only. This file IS committed.
# Secrets (GEMINI_API_KEY, AUTH_SECRET) go in .env.local, which is gitignored.

# SQLite database location (relative to the prisma/ directory).
DATABASE_URL="file:./dev.db"
